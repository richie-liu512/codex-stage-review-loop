interface:
  display_name: "Acceptance Review"
  short_description: "Lightweight acceptance review and bounded fix loops"
  brand_color: "#2563EB"
  default_prompt: "Use $stage-review-loop to verify the current work against my request, check real evidence, fix blocking issues, and stop if a decision needs my confirmation."

policy:
  allow_implicit_invocation: true
